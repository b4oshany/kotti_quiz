<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xml:lang="en"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      metal:use-macro="api.macro('kotti:templates/view/master.pt')">

  <head tal:omit-tag="" metal:fill-slot="head-more">
    <link rel="stylesheet" href="${request.static_url('kotti_quiz:static/style.css')}" type="text/css" />
    <more tal:omit-tag="" metal:define-slot="head"></more>
  </head>

  <div metal:fill-slot="content" class="content">

    <h1>${context.title}</h1>
    <p class="description">
      ${context.description}
    </p>

    <ol>
      <li tal:repeat="question questions">
        ${question.title}
        <p class="rightanswer" tal:condition="questioncorrect[question.name]">
          Antwort korrekt
        </p>
        <p class="wronganswer" tal:condition="not questioncorrect[question.name]">
          Leider falsch <br>
          Die korrekte Antwort lautet: ${question.correct_answer}
        </p>
      </li>
    </ol>
  Insgesamt: ${sumcorrect}/${sumtotal}

  </div>
</html>
